services:
  - type: web
    name: nome-do-seu-servico
    env: python
    buildCommand: |
      # Instalar dependências do sistema
      apt-get update && apt-get install -y portaudio19-dev
      pip install -r requirements.txt
    startCommand: python seu_script.py
